Meteor.startup(function() {
	
	/*
	var id = Ghars.insert({title:"Sagar",loc:{type: "Point", coordinates:[0, 0]}}, function(e,r){
		if (e != null)
			alert(e);
	});
	
  $('body').on('click', '[data-action=logout]', function(event) {
    event.preventDefault();
    AccountsTemplates.logout();
  });
  */
});
